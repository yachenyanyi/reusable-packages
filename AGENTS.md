# 公共组件仓库规则

本仓库包含可复用组件。`packages/` 下的每个子目录都应当能够独立理解，也可以在未来独立发布；在没有真实需要前，它们共同使用一个 Git 仓库。

- 每个组件拥有自己的 `pyproject.toml`、源码包、测试、文档和公共 API。
- 不要把无关库混进某个组件的 `src` 目录。
- 跨组件引用必须使用文档化的公共 API，并声明明确依赖；不能导入私有模块。
- 组件依赖应保持精简；供应商 SDK 和基础设施客户端放入可选适配器或 extras。
- 不要仅因为代码可能复用就拆新组件，必须有稳定用途以及真实的复用、依赖、发布或维护压力。
- 非简单的边界或 API 变更，先设计和记录契约，再实现和测试。
- 可复用库不能反向依赖应用代码。
- 公共组件文档不收录客户项目反馈、现场验收记录或临时运行问题；这类内容放在 `apps/<project>/feedback/`。
- 确认属于公共组件的缺陷，应在组件所属 Issue 跟踪修复；必要的架构决策或稳定契约变更再沉淀到组件自己的 `docs/`。

监测组件位于 `monitoring-kit/`，其核心和扩展规则见 `monitoring-kit/docs/`。
